# Headers Settings

The Headers settings contains the mapping for the column names of the file, this allows the
program to read and parse the expected data from the file.

The column values are ***case insensitive***.